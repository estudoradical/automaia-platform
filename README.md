# Automaia Platform

Plataforma de automação inteligente para gestão de tráfego.

## 🚀 Começando

```bash
# Instalar pnpm
npm install -g pnpm

# Instalar dependências
pnpm install

# Configurar environment
cp .env.example .env

# Setup database
pnpm db:generate
pnpm db:push

# Rodar desenvolvimento
pnpm dev
